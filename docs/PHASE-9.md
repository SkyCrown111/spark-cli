# Phase 9 — 可视化关卡与动画

> **前置**：Phase 2 ✅  
> **状态**：✅ 已完成

## 交付

- [x] `spark-cli level` / `spark-cli anim`
- [x] `spark-cli editor serve`（Web UI localhost）
- [x] 与 staging 双向同步

## 验收

- [x] Cocos fixture 生成可运行关卡数据 + 状态机模板

验收：`pnpm test:phase9`
